import { add, subtract } from "./calculator.js";

// Adding two numbers using the add function and logging the result
const sum = add(5, 3);
console.log("Sum:", sum);

// Subtracting two numbers using the subtract function and logging the result
const difference = subtract(8, 4);
console.log("Difference:", difference);
